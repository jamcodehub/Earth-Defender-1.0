to get the .exe.

1. Open terminal from folder
  
2. type "npm install" > enter
   
   Windows install
3. type "npm run electron:build:win" > enter
   Mac install
4. type "npm run electron:build:mac" > enter

5. navigate on the folder earth-defence/release/win-arm64-unpacked/Earth Defense Net.exe
